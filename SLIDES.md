# 👁️ SmartEye 발표자료
> **실시간 졸음 & 집중력 저하 감지 시스템**  
> Python · MediaPipe · OpenCV · EAR 알고리즘

---


# 🎯 프로젝트 목표 및 배경

* **해결하고자 하는 문제**: 장시간 운전 및 컴퓨터 업무 시 발생하는 <span class="highlight">피로와 집중력 저하</span>
* **목표**: 일반 웹캠(Webcam) 하나만으로 동작하는 가볍고 정확한 실시간 감지 소프트웨어 구축
* **차별점**:
  1. **개인화 임계값 자동 보정 (Calibration)**
  2. **비동기 오디오 처리**를 통한 프레임 드랍 방지
  3. **이동 평균 필터**를 사용한 오탐률 최소화

---

# 🔬 핵심 감지 알고리즘 (EAR)

눈 주변의 6개 얼굴 랜드마크 사이의 거리 비율 연산
$$EAR = \frac{||P_2 - P_6|| + ||P_3 - P_5||}{2 \times ||P_1 - P_4||}$$

* **Awake (정상)**: 눈이 열려 있어 높은 EAR 유지 ($0.25 \sim 0.35$)
* **Drowsy / Sleeping**: 눈이 닫히며 EAR이 급격히 감소 ($< 0.20$)
* **기하학적 특징**: 얼굴의 크기, 카메라와의 거리에 독립적인 정규화된 값 획득 가능

---

# 🧠 자동 개인화 보정 (Calibration)

사용자별 안구 구조 편차 극복을 위한 필수 단계

1. **초기 수집 단계 (5초)**
   * 편안하게 화면을 응시하며 실시간 EAR 데이터 수집
2. **보정 임계값 산출**
   * $\text{Threshold} = \text{Average Base EAR} \times 0.89$
3. **최소 안전 장치**
   * 보정값이 너무 낮게 측정되더라도 최소 수치($0.18$) 미만으로 떨어지지 않도록 설계해 최저 민감도 보장

---

# ⚙️ 시스템 구조 & 파이프라인

계층형 구조 설계로 결합도를 낮추고 모듈 독립성 확보

```
[Camera Input] ──> [Face Mesh Detector] ──> [Eye EAR Analyzer]
                                                  │
                                                  ▼
[UI / Overlay] <── [Alert Manager] <── [Drowsiness Classifier]
     │
     └──> [CSV Logger / Async Audio]
```

* **입력**: OpenCV 카메라 스레드
* **감지**: MediaPipe Face Mesh 모델 (468 랜드마크 추출)
* **판단**: 시계열 윈도우 기반 상태 변화 감지 및 동기화

---

# 📊 3단계 피로 분류 체계

사용자의 졸음 깊이에 따른 실시간 대처 시나리오

* **😀 Awake (정상)**: EAR이 임계값 이상 유지되는 상태
* **😪 Drowsy (졸음 의심)**: 
  * 임계값 미만 상태 <span class="yellow">1초 연속 지속</span> 시 진입
  * 노란색 화면 가이드 및 경고 알림음 1회 발생
* **😴 Sleeping (수면 상태)**:
  * 임계값 미만 상태 <span class="red">3초 연속 지속</span> 시 진입
  * 적색 화면 점멸 및 강력한 경보 사이렌 지속 재생

---

# 🛠️ 비차단 비동기 오디오 아키텍처

경고 오디오 재생 시 발생하는 스레드 블로킹 문제 해결

```
[메인 비전 스레드 (30 FPS 유지)] ── 경고음 트리거 ──> [Audio Queue]
                                                              │
                                                        (비동기 스레드)
                                                              ▼
                                                        [오디오 디바이스]
```

* **기존 동기화 방식**: `winsound` 또는 오디오 재생 라이브러리가 프레임을 대기시켜 화면 멈춤 발생
* **개선 비동기 방식**: 재생 작업을 별도 스레드로 분산하여 웹캠 렌더링 루프의 지연(Delay)을 완벽히 방지

---

# 📈 데이터 로깅 및 분석 활용

상태 변화 및 원본 EAR의 정량적 데이터를 CSV 형태로 저장

* **위치**: `logs/session_*.csv`
* **저장 항목**: 
  * 타임스탬프 (Timestamp)
  * 이전 상태 $\rightarrow$ 변경된 상태
  * 상태 지속 시간 (Duration)
  * 해당 세션의 평균 EAR 수치
* **기대 효과**: 운전자 피로 축적 시각화 및 집중력 저하 분석 통계 데이터 제공

---

# 🏁 프로젝트의 미래 & 개선 로드맵

* **저조도 성능 개선**: 야간 주행 등 어두운 환경에서의 인식을 보완하기 위한 조도 센서 연동 또는 전처리 필터 탑재
* **다중 바이오메트릭 결합**:
  1. 눈 깜빡임 빈도 (Blink Rate)
  2. 하품 감지 (Yawn Detection)
  3. 헤드 포즈 기반 시선 이탈 감지 (Gaze Track)
* **최종 목표**: 모바일 및 임베디드 기기 이식 버전 릴리즈

---

# 💡 질의응답 (Q&A)

* 발표를 경청해 주셔서 감사합니다.
* **프로젝트 소스코드 저장소**:
  * [GitHub Repository](file:///d:/SmartEye-Drowsiness-Detection-Python-)
* 질문이 있으시면 편하게 말씀해 주세요.
