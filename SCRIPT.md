# SmartEye 발표 스크립트 (초간단/직관적 톤)

> **특징**: 복잡한 전공 기술 용어를 싹 빼고, 웹캠 화면의 동작 원리와 파일 역할만 상식 선에서 간결하게 읽을 수 있도록 만든 초단축 대본입니다.

---

## 1. 개요 및 기술 스택
본 프로젝트는 웹캠 영상을 통해 사용자의 눈 감김 상태를 감지하고 경고를 주는 졸음 예방 프로그램입니다.

기술 스택은 파이썬을 기반으로 하며, OpenCV를 사용해 영상 화면을 켜고 경고 메시지를 렌더링합니다. 얼굴 인식에는 MediaPipe Face Mesh라는 인공지능 모델을 가져와 얼굴 위에 468개 점들을 찍어 사용하며, 수치 계산은 NumPy 라이브러리로 처리합니다.

---

## 2. 작동 원리 (EAR 및 자동 보정)
눈 감김을 판단하기 위해 눈의 가로 길이 대비 세로 길이 비율을 뜻하는 EAR(Eye Aspect Ratio) 공식을 사용합니다. 
눈을 정상적으로 뜨고 있으면 세로가 길어지므로 수치가 높게 유지되고, 눈을 감으면 세로 거리가 줄어들며 0에 가까워집니다. 이 원리를 사용하므로 머리를 조금 움직이거나 카메라와 가까워져도 오류 없이 눈 감김을 측정할 수 있습니다.

또한 사람마다 평소 눈 크기가 다른 점을 해결하기 위해 실행 첫 5초간 눈을 편하게 뜨고 있는 상태를 수집합니다. 5초 뒤 그 평균값을 기준으로 삼아 졸음을 판단할 개인 맞춤형 임계값을 자동 계산해 냅니다.

---

## 3. 프로그램 파일 구성과 역할
프로그램 내부 파일들은 유지보수를 편하게 하기 위해 각자 자기 일만 하도록 설계되었습니다.

*   **main.py**: 프로그램의 메인 실행 루프를 시작하고 통제합니다.
*   **camera.py**: 웹캠 장치를 연결하고 실시간 화면을 가져옵니다.
*   **detector.py**: 영상에서 얼굴을 탐지하고 눈 주변 랜드마크 점들의 위치를 골라냅니다.
*   **analyzer.py**: 눈 점들의 가로세로 거리를 기반으로 비율(EAR)을 계산해 줍니다.
*   **classifier.py**: 계산된 눈 크기 수치를 보고 눈 감은 시간을 측정해 최종적으로 졸음인지 정상인지를 판별합니다.
*   **alert.py**: 졸음이 감지되면 소리를 켜라는 신호를 보냅니다.
*   **renderer.py**: 웹캠 화면에 눈 윤곽선 점들을 그리고 노란색/빨간색 경고 테두리를 입힙니다.
*   **utils/audio_player.py**: 소리를 백그라운드 스레드에서 재생하여, 경고음이 울릴 때도 카메라 화면이 버벅이거나 멈추지 않게 방지합니다.
*   **utils/logger.py**: 사용자가 졸았던 타임스탬프와 지속 시간 등을 CSV 파일에 실시간 누적하여 저장합니다.

---

## 4. 3단계 경고 단계 및 비동기 사운드
저희 프로그램은 눈을 감은 시간초에 따라 3단계로 동작합니다.
평소에는 Awake(정상)이다가, 눈을 감은 지 1초가 넘어가면 Drowsy(졸음 진입) 상태가 되어 화면에 노란색 테두리가 그려지고 1회성 비프음이 울립니다. 만약 3초 이상 계속 감고 있으면 Sleeping(위험 수면) 단계로 전환되며 화면이 빨갛게 깜빡이고 사이렌 경고음이 반복해서 출력됩니다.

이때 소리가 날 때 화면 프레임이 버벅거리지 않도록, 사운드 재생 코드는 프로그램 메인 흐름과 별도의 스레드에서 독자적으로 동작하도록 프로그래밍했습니다.

---

## 5. 실행 방법 및 마무리
가상환경을 만들고 필요한 패키지들을 설치한 후 `python main.py`를 터미널에 입력하면 즉시 구동됩니다.
*   시작하자마자 첫 5초 동안 정면을 똑바로 바라보면 **자동 보정**이 끝납니다.
*   사용 도중 환경이 달라져서 다시 보정하고 싶다면 영문 `c` 키를 누르면 되고, 
*   프로그램을 끌 때는 영문 `q` 키를 누르면 안전하게 종료됩니다.

이상으로 실시간 졸음 감지 시스템 SmartEye의 발표를 마치겠습니다. 질문이 있으시면 답변드리겠습니다. 감사합니다.
